using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class canvasContainerScript : MonoBehaviour
{
    //public Transform Camera;
    //// Start is called before the first frame update
    //void Start()
    //{
        
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    transform.position = Camera.position;
    //    transform.rotation = Quaternion.Euler(transform.rotation.x, Camera.rotation.eulerAngles.y, transform.rotation.x);
    //}
}
